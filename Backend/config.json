{
    "database": {
      "host": "${DATABASE_HOST}",
      "port": "${DATABASE_PORT}",
      "user": "${DATABASE_USER}",
      "password": "${DATABASE_PASSWORD}",
      "database": "${DATABASE_NAME}"
    },
    "secret": "${JWT_SECRET}",
    "emailFrom": "${EMAIL_FROM}",
    "smtpOptions": {
      "host": "${SMTP_HOST}",
      "port": "${SMTP_PORT}",
      "auth": {
        "user": "${SMTP_USER}",
        "pass": "${SMTP_PASS}"
      }
    }
  }